const { Router } = require("express");
const router = Router();
const chatFunctions = require("../src/controllers/chat");

// router.post("/getMessage/:clinetId", chatFunctions.getMessage);

module.exports = router;
